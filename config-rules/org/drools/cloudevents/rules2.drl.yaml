
name: org.drools.cloudevents
rules:
- name: configured rule
  when:
  - given: fact.Measurement
    as: $m
    having:
    - "id == \"color\""
  then: |
    results.put("configured rule by operator result2", $m.val + "!!");
